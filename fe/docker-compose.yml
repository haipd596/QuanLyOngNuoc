services:
  frontend:
    image: ${DOCKER_IMAGE}:${DOCKER_TAG}
    ports:
      - "${APP_PORT}:80"
    restart: unless-stopped
    networks:
      - shared-net

networks:
  shared-net:
    external: true
    name: shared-net
