import { Input, InputProps } from 'antd';
import React from 'react';

const PhoneNumberBuider = (props: InputProps) => {
  return (
    <Input {...props} />
  );
};

export default PhoneNumberBuider;
