import { Input, InputProps } from 'antd';
import React from 'react';

const PhoneNumberViewer = (props: InputProps) => {
  return (
    <Input {...props} />
  );
};

export default PhoneNumberViewer;
