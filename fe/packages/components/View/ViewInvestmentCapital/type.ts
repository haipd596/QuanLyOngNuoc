import { AnyObject } from 'antd/es/_util/type';

export type TViewInvestmentCapitalProps = {
  fieldKey: string;
  onChange: (data: AnyObject[]) => void;
  value: any;
  modeView: string;
  name: string;
};
